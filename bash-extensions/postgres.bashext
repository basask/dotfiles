#!/bin/bash

# Host
export PGHOST=localhost

# Database
export PGDATABASE=

# user
export PGUSER=postgres

# Create ~/.pgpass with the following format:
# host:port:database:postgres:postgres
# Ex.: *:*:*:postgres:postgres
